Setup
Download the package to your local machine
Unzip
Open index.html using any browser (Chrome, Firefox...)
Play the game

Game Rules
Each user can hit the roll button
When a user gets 15 or more point, that user wins
If the user rolls a 1 dice, the user loses all the points
When a user clicks hold, it is the other user's turn to play the game
